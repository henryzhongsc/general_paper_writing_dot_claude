---
description: Create empty commit as version pin
---
Create an empty commit using `git commit --allow-empty` with the message prefixed by 📌. Concatenate with user input and optionally polish for clarity. Then push. Always show the commit message in the summary.
